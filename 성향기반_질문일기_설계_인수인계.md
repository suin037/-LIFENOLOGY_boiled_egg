# Parallel Me — 성향 기반 "질문형 일기" 설계 · 인수인계 브리프

> 다른 Claude 세션에 붙여넣어 이어서 작업하기 위한 자기완결 문서.
> 이 문서 하나로 맥락 파악 + 다음 작업 진행이 가능하도록 정리했습니다.
> 작성 2026-07-26.

---

## 0. 이 문서의 목적

`jy-model` 브랜치의 **일기 감정분석 시스템**을, 자유 일기가 아니라 **성찰 질문에 답하는 형식**으로 바꾸고, 그 답변에서 **유저 성향을 추출해 미래 예측 시나리오(Parallel Me) 서사를 개인화**하는 방향을 설계했다. 이 문서는 그 설계 결정과 구현 계획을 담는다.

---

## 1. 프로젝트 배경 (최소 맥락)

- **Parallel Me / LIFENOLOGY** — 삶의 갈림길(이직·창업·진학)에 선 20~30대를 위한 AI 인생경로 시뮬레이터. "선택을 대신 하지 않고 돕는다"가 원칙.
- **예측 엔진**(backend): 한국 패널데이터(KLIPS·GOMS·YP)로 4계층 추정 — L1 룰베이스 / L2 KNN 유사인물 / L3 EconML 인과추론 / L4 lifelines 생존. 수치를 Claude API가 서사로 변환.
- **차별점**: 경쟁 서비스(Parallel Lives 등)가 "AI가 지어낸 이야기"인 반면, 이쪽은 "실제 패널 데이터에서 뽑은 수치를 이야기로 변환". → **이 원칙을 깨는 설계는 지양**(뒤 §3-금지 참조).
- **일기 모듈**(diary_module): 일기 텍스트 → 감정 6종·VAD·언어지표 → 심리 이론카드 RAG → 웰빙 리포트. 원래는 유저 이해용 웰니스 인풋.

---

## 2. 저장소 실측 검증 결과 (이미 확인 완료 — 신뢰 가능)

저장소 `github.com/suin037/-LIFENOLOGY_boiled_egg`(공개)를 실제로 조회해 설명 문서와 대조함. **문서는 신뢰할 만함.** 어긋난 건 아래 하나뿐.

- ⚠️ **`emotion_taxonomy.json`의 상황(situation) 개수가 문서엔 "12"인데 실제는 13개.** → 문서 §1 수정 필요(사소).
- 나머지(infer.py `analyze()`, hybrid.py `ESCALATE_LABELS={"상처","당황"}`·`CONF_THRESHOLD=0.50`, psych_link 안전게이트·프록시·근거블록, weekly_report `--metrics`·수면↔기분 상관 `np.corrcoef`)는 코드가 문서 서술과 일치.

### RAG(minjub423 `lanollab-data` 브랜치)에 실제로 든 자료
- **심리 이론카드 16장** (`data/lanollab/심리학_이론카드/`): 대처 3(Lazarus & Folkman 1984) + 긍정정서 8(Fredrickson) + 회복탄력성 4(Connor & Davidson CD-RISC).
  - 각 카드는 `emotion_keywords`(일기 감지 신호어) + `interventions` + `narrative_guidance` + `source`(논문 인용) 구조.
  - **주의**: 빌드 스크립트가 비재귀 glob이라 하위폴더 `_handoff_sohyunio/`의 **회복탄력성 4장은 벡터DB에 안 실림**. 현재 적재 = 대처 3 + 긍정정서 8 = **11장**뿐. 확장하려면 파일을 상위 폴더로 옮겨 재빌드.
- **건강·웰빙 RAG 청크 72개**(KNHANES 20·CHS 20·KWCS 16·웰빙논문 16) + lookup 4개. **존재하지만 심리카드 벡터DB엔 아직 미통합**(향후 웰빙 근거 강화용).

### 리포트 신뢰도 판정
- **믿을 부분**: 인용 이론은 실제 논문(카드 `source`), 감정·valence는 로컬 모델(긍/부정 F1 0.952), 수면↔기분 상관은 실제 계산값.
- **과신 금지**: 상관은 n=7일치라 통계적으로 취약 → "인과"가 아니라 "그 주 패턴 묘사"로 읽을 것. 이론-상황 매칭은 Claude 해석이지 검증된 처방 아님. 위기(⚠️)가 뜬 주는 낙관 톤을 자제할 것. 감정 모델은 진단 도구가 아님.

---

## 3. 이번 세션의 핵심 설계 결정

### 결정 1 — 성향은 "예측 수치"가 아니라 "서사"에만 반영
예측 수치(KNN/EconML/lifelines가 뽑는 소득·삶의질·생존 궤적)는 **패널 데이터 그대로 두고 절대 건드리지 않는다.** 성향은 그 수치와 Claude 서사 사이에 **"성향 렌즈"**를 끼워 세 가지만 조정: (a) 5개 비교축(성장·안정·관계·자기실현·경제) 중 무엇을 먼저·크게 보여줄지, (b) 서사 프레임·톤(위협↔도전 등), (c) 어떤 심리카드·"준비" 항목을 붙일지.
- **같은 이직 예측, 같은 숫자라도 성향에 따라 읽히는 시나리오가 달라진다** = 차별점의 실체.
- **금지**: 성향을 KNN 매칭 피처나 EconML 입력으로 넣지 말 것. 패널 응답자에겐 성향 라벨이 없어 검증 불가 → 추정치 오염 + "데이터 없이 지어낸 예측"이 되어 차별점 자기부정.
- **불리한 축을 숨기지 말 것**: 안정 지향 유저에게도 리스크는 다 보여주되 순서·톤만 조정. 선 넘으면 "듣기 좋은 말만 하는 서비스"가 됨 → 원칙 위반.

### 결정 2 — MBTI 안 씀
MBTI는 재검사 신뢰도 낮고 성과 예측력 약함. 성향 추출 소스로는 **일기 언어지표**(이미 있는 `diary_module/metrics.py`: 1인칭·절대어·통찰·대처균형·정서극성, Tausczik & Pennebaker 2010 근거)를 쓴다. 원하면 MBTI는 재미 요소로만, 예측 반영은 안 함. (성향을 꼭 문항으로 받고 싶으면 Big Five가 대안.)

### 결정 3 — 자유 일기 → "질문형 성찰 일기"로 전환 (이번 세션의 중심)
빈 종이 자유 일기는 문체·길이 제각각이라 신호가 불안정하고 이탈률 높음. **성찰 질문으로 유도하면** 답변 형식이 정렬돼 감정 모델·언어지표가 안정적으로 작동하고, 성찰 질문 자체가 통찰 언어를 끌어냄. 참고로 유저가 원하는 목표 글 품질은 "'피곤하다'가 내 하루 전체를 설명하는 단어가 되어버렸다…"류의 앵커+메타포+통찰이 담긴 반추적 글(별도 예시 이미지 있었음).
- **핵심 원칙**: 질문은 **"성향 측정 문항"이 아니라 "정제된 입력을 만드는 성찰 유도 장치"**. 성향은 여전히 답변 텍스트에서 감정 모델+언어지표로 추출. (질문을 설문으로 착각하면 MBTI 문제로 회귀.)
- **좋은 질문 = 닻(단어·순간·장면) + "왜/어디에 번지는가"**. "오늘 어땠어요?"(→"그냥요") 대신 "요즘을 한 단어로? 그게 어디에 스며 있나요?"

### 결정 4 — 질문 풀 3층 구조
| 층 | 성격 | 목적 | 예 |
|---|---|---|---|
| 매일 코어 | 앵커·짧게, 매일 반복(시계열 비교) | valence 앵커 + 통찰 | "요즘 내 하루를 한 단어로? 그게 어디에 스며 있나요?" |
| 가벼운 성찰(취향) | 부담 낮음, 리텐션 | 취향을 우회로 성향 신호 수집 | "최근 꽂힌 영화·책·애니·영상 하나 + **어떤 장면/인물이 남았나요?**" |
| 주간 심층 | 직접적, 주 1~2회 | 성향·가치관 추출 | "최근 망설인 선택은? 뭐가 마음에 걸렸나요?" / "삶에서 늘리고·줄이고 싶은 것은?" |
- 성향 추출 = **심층·취향 답변의 언어지표 누적 평균** → 별도 성향 설문 불필요.
- **취향 질문 주의**: 반드시 "왜/어떤 장면"을 붙일 것. "무엇을 봤나"만 물으면(예: "오징어게임요") 감정·지표·카드 다 안 잡히는 빈 입력.

### 결정 5 — 유튜브 링크 등은 "첨부 옵션"으로만
링크 자체는 시스템에 빈 입력(감정·형태소·카드 없음). → 별도 질문 만들지 말고 **기존 취향 질문의 선택적 첨부 옵션**으로 흡수("영상 뭐든, 최근 꽂힌 거 하나 + 왜"). 링크 메타데이터/자막 분석은 MVP엔 과설계.
- ⚠️ **프라이버시**: 시청 이력은 취향보다 민감(정치·건강 성향 노출). 삼성생명 스폰서 서비스이므로 수집·보관·동의 정책이 확실해야 하고, 안 넣어도 서비스가 돌아가게 설계.

---

## 4. 구현 계획 (관할: backend 수정은 담당자 조율 필요)

지금 `backend/utils/claude_api.py`의 `generate_narrative()`는 수치 4개만 넣는 단순 프롬프트라 성향이 전혀 안 들어감. 아래로 성향 렌즈를 합류.

**1) `schemas.py` `PredictRequest`에 필드 추가**
```python
class ValueProfile(BaseModel):        # (선택) 온보딩 질문에서 왔거나 생략 가능
    stability_vs_challenge: float = Field(0.5, ge=0, le=1)
    growth_vs_balance: float      = Field(0.5, ge=0, le=1)
    relation_vs_autonomy: float   = Field(0.5, ge=0, le=1)
    meaning_vs_reward: float      = Field(0.5, ge=0, le=1)
    intuitive_vs_analytic: float  = Field(0.5, ge=0, le=1)
# PredictRequest에:
    value_profile: Optional[ValueProfile] = None
    diary_metrics: Optional[dict] = None   # metrics.analyze_text 누적 평균
```

**2) 언어지표는 기존 `diary_module/metrics.py` 재활용** (신규 코드 없음)
`analyze_text(text)` → `first_person_ratio·absolutist_ratio·insight_ratio·coping_balance·emotion_density`. 일기(=질문 답변)가 쌓이면 이 값들을 평균 내 `diary_metrics`로 전달. 신규 유저는 비워두면 질문값만으로 동작.

**3) 신규 `backend/utils/persona.py` — 성향 렌즈 (이 설계의 핵심)**
값 → 자연어 지시문. 수치는 안 건드림.
```python
def build_persona_lens(value_profile, diary_metrics) -> dict:
    vp, dm = value_profile or {}, diary_metrics or {}
    axis_weight = {
        "안정": 1 - vp.get("stability_vs_challenge", 0.5),
        "성장": vp.get("growth_vs_balance", 0.5),
        "관계": 1 - vp.get("relation_vs_autonomy", 0.5),
        "자기실현": vp.get("meaning_vs_reward", 0.5),
        "경제": 1 - vp.get("meaning_vs_reward", 0.5),
    }
    top_axes = sorted(axis_weight, key=axis_weight.get, reverse=True)[:2]
    directives = []
    if dm.get("absolutist_ratio", 0) > 0.015:
        directives.append("흑백사고 경향 → 단정 표현 피하고 해석 여지 넓혀 서술.")
    if dm.get("coping_balance", 0) < -0.2:
        directives.append("회피 대처 → 문제를 작게 쪼갠 첫 행동 하나 제안.")
    elif dm.get("coping_balance", 0) > 0.2:
        directives.append("능동 대처 → 도전·실험 프레임으로 서술.")
    if dm.get("first_person_ratio", 0) > 0.05:
        directives.append("자기몰입·반추 → 관점 전환/사회적 지지 넛지.")
    if vp.get("stability_vs_challenge", 0.5) < 0.4:
        directives.append("안정 지향 → 리스크는 숨기지 말되 '재구성' 관점으로 먼저 제시.")
    return {"top_axes": top_axes, "directives": directives}
```

**4) `generate_narrative()` 프롬프트에 성향 블록 합류** (수치 부분 불변)
```python
lens = build_persona_lens(req.value_profile, req.diary_metrics)
persona_block = ""
if lens["directives"]:
    persona_block = (f"\n[사용자 성향 — 서술 방식에만 반영, 수치 불변]\n"
                     f"- 먼저 강조할 축: {', '.join(lens['top_axes'])}\n"
                     + "".join(f"- {d}\n" for d in lens["directives"]))
# 기존 prompt에 persona_block 삽입 + "수치를 바꾸지 말고 성향에 맞는 프레임·순서·톤으로,
#  불리한 축도 숨기지 말 것" 지시 추가.
```

**5) 질문 텍스트를 답변과 함께 파이프라인에 전달**
`analyze_hybrid(text)`에 `question` 인자 추가 → 감정·서사 프롬프트에 얹기. 답만 보면 "무엇에 대한 답인지" 소실. 질문을 알면 "'망설인 선택'을 물었을 때 리스크 회피 언어가 반복됐다"식 정확한 서술 가능. 주간 리포트도 항목에 질문 라벨 부착.

**안전**: 심층 질문("힘든 고민")은 위기 신호를 끌어낼 수 있음 → `crisis.py` + `rag.safety` 게이트 반드시 태워 무거운 답이 나오면 상담자원 분기(리포트가 가볍게 넘기지 않도록).

---

## 5. 관할·의존성 메모

- diary_module = 이쪽 관할. backend(`claude_api.py`·`schemas.py`) 수정은 backend 담당과 조율. 이쪽에선 "언어지표 누적 평균 헬퍼"까지 만들어 넘기고 프롬프트 합류는 공동 진행 권장.
- RAG 백엔드(`backend/rag/`)·이론카드는 `lanollab-data`(minjub423) 소재. 복사만, 수정 금지.
- API 주의: 최신 모델은 `temperature/top_p/top_k` 거부(400) → 넣지 말 것. 리포트 생성기는 `thinking={"type":"disabled"}`로 잘림·비용 방지.

---

## 6. 다음에 만들 것 (진행 중이던 다음 스텝)

1. **질문 풀 전체 문서** — 3층(매일 코어 / 가벼운 성찰·취향 / 주간 심층), 코어 2~3개 + 취향·심층 로테이션 10개 내외. **각 질문마다 겨냥 언어지표·성향축·위기위험도 매핑 표**로. 유튜브는 취향 질문의 첨부 옵션 + 프라이버시 주의 반영. → *이 문서를 만드는 게 바로 다음 작업이었음.*
2. `persona.py` 실제 파일화(값→지시문 매핑 확정).
3. 온보딩 질문 5문항 실제 문구(슬라이더 라벨 포함) — 질문형 일기로 상당 부분 대체 가능하므로 최소화 검토.
4. (문서 수정) `JYMODEL_전체설명.md` §1의 상황 "12→13" 반영.
